{
  "compilerOptions": {
    "allowJs": true,
    "checkJs": false,  // Set to true if you want TypeScript to check JavaScript files
    "jsx": "react-jsx", // or "react" if you are using React without the new JSX transform
    "resolveJsonModule": true, // Optional: if you import JSON modules
    // other options...
  },
  "include": ["src/**/*"]
}